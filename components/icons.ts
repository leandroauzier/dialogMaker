// Centralized lucide-react icon exports used across DialogForge.
// Import icons from here instead of "lucide-react" directly so the
// set of icons in use stays easy to audit.
export {
  // Top bar / project actions
  MessagesSquare,
  Save,
  FolderOpen,
  Download,
  Play,
  X,

  // Node palette / node headers
  MessageCircle,
  GitFork,
  HelpCircle,
  Keyboard,
  Zap,
  Flag,

  // Boolean node true/false
  Check,

  // Sidebar actions
  Settings,
  Trash2,
  Copy,
  Star,

  // Preview modal
  AlertTriangle,
  ArrowRight,
  RotateCcw,

  // Auth / dashboard
  LogOut,
  LayoutDashboard,
  Plus,
  Trash,
} from "lucide-react";
